= render "shared/second-header"
= form_with class: 'registration-main', local: true do |f|
  .form-wrap
    .form-header
      %h1.form-header-text
        会員情報入力
    .form-group
      .form-text-wrap
        %label.form-text ニックネーム
        %span.indispensable 必須
      = f.text_area :hoge, class:"input-default", placeholder:"例) furima太郎", maxlength:"40"
    .form-group
      .form-text-wrap
        %label.form-text メールアドレス
        %span.indispensable 必須
      = f.email_field :hoge, class:"input-default", placeholder:"PC・携帯どちらでも可", autofocus: true
    .form-group
      .form-text-wrap
        %label.form-text パスワード
        %span.indispensable 必須
      = f.password_field :hoge, class:"input-default", placeholder:"6文字以上の半角英数字"
      %p.info-text ※英字と数字の両方を含めて設定してください
    .form-group
      .form-text-wrap
        %label.form-text パスワード(確認)
        %span.indispensable 必須
      = f.password_field :hoge, class:"input-default", placeholder:"同じパスワードを入力して下さい"
    .form-group
      %p.form-info-header
        本人確認
      %p.form-info-text
        安心・安全にご利用いただくために、お客さまの本人情報の登録にご協力ください。他のお客さまに公開されることはありません。
    .form-group
      .form-text-wrap
        %label.form-text お名前(全角)
        %span.indispensable 必須
      .input-name-wrap
        = f.text_area :hoge, class:"input-name", placeholder:"例) 山田"
        = f.text_area :hoge, class:"input-name", placeholder:"例) 陸太郎"
    .form-group
      .form-text-wrap
        %label.form-text お名前カナ(全角)
        %span.indispensable 必須
      .input-name-wrap
        = f.text_area :hoge, class:"input-name", placeholder:"例) ヤマダ"
        = f.text_area :hoge, class:"input-name", placeholder:"例) リクタロウ"
    .form-group
      .form-text-wrap
        %label.form-text 生年月日
        %span.indispensable 必須
      .input-birth-wrap
        = raw sprintf(                               |
          f.date_select(                             |
            :hoge,                                   |
            class:'select-birth',                    |
            use_month_numbers: true,                 |
            prompt:'--',                             |
            start_year: 1930,                        |
            end_year: (Time.now.year - 5),           |
            date_separator: '%s'),                   |
          "<p> 年 </p>", "<p> 月 </p>") + "<p> 日 </p>" |
      %p.info-text ※ 本人情報は正しく入力してください。会員登録後、修正するにはお時間を頂く場合があります。
    .form-group
      %h2.form-bottom-text
        「会員登録」のボタンを押すことにより、
        %span 利用規約
        = succeed "\u{306b}\u{540c}\u{610f}\u{3057}\u{305f}\u{3082}\u{306e}\u{3068}\u{307f}\u{306a}\u{3057}\u{307e}\u{3059}" do
          %br/
    .register-btn
      = f.submit "会員登録" ,class:"register-red-btn"
    %p.register-info 本人情報の登録について
= render "shared/second-footer"
